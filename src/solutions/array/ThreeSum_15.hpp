#include "src/utilities/CommonHeaders.hpp"

/**
 * @details Key point is to remove duplication
 * Maybe removing duplication has something to do with double pointers
 */
class ThreeSum {
public:
    vector<vector<int>> operator()(vector<int> &nums);

};