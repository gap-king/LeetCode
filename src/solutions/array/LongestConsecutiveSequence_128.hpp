#ifndef LONGEST_CONSECUTIVE_SEQUENCE
#define LONGEST_CONSECUTIVE_SEQUENCE

#include <vector>

extern const int aa;
extern int iii;


class LongestConsecutiveSequence {
public:
    int Solve(const std::vector<int> &nums);

    // more easy
    int Solve2(const std::vector<int> &nums);
};

#endif