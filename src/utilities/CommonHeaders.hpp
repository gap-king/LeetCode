#ifndef COMMON_HEADERS
#define COMMON_HEADERS

#include <algorithm>
#include <cstdint>
#include <limits>
#include <string>
#include <vector>

using std::vector;
using std::string;

/// @brief Define test case index
enum struct TestCase {
    zero,
    one,
    two,
    three,
    four,
    five
};


#endif